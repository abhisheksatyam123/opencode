/**
 * Foundation L0 — Adapter placeholder.
 *
 * Foundation has no Effect services and no DI dependencies; FoundationLayer
 * (in ./layer.ts) is `Layer.empty`. This file exists to satisfy the AC-1
 * module-architecture invariant that every module exposes a uniform
 * port/adapter/layer/index quartet, and to provide a single import target
 * for callers that mirror the pattern across modules.
 *
 * No runtime exports — the public surface is re-exported from ./index.ts.
 */
export {}
