/**
 * Tool L3 — compatibility layer barrel
 *
 * New module-shape code lives in `wiring/layer.ts`; this file preserves
 * existing imports from `@/tool/layer`.
 */

export { ToolLayer } from "@/tool/wiring/layer"
export { Tool } from "@/tool/contract/port"
export { ToolRegistry } from "@/tool/registry"
export { ToolCard } from "@/tool/card"
